$("#x").html(
    $("#x").text().split("").map(a => `<span class="letterHover">${a}</span>`)
)

$("#y").html(
    $("#y").text().split("").map(a => `<span class="letterHoverYZ">${a}</span>`)
)

$("#z").html(
    $("#z").text().split("").map(a => `<span class="letterHoverYZ">${a}</span>`)
)